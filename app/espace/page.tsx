import { PageProvisoire } from "@/components/PageProvisoire";

export const metadata = { title: "Espace client" };

export default function PageEspaceClient() {
  return (
    <PageProvisoire
      titre="Espace client"
      description="Connexion, suivi de commandes, devis et factures — module Authentification + Devis/Factures, phase 3."
    />
  );
}
