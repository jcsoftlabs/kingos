import { PageProvisoire } from "@/components/PageProvisoire";

export const metadata = { title: "Contact" };

export default function PageContact() {
  return (
    <PageProvisoire
      titre="Contactez Kingo's"
      description="Formulaire de contact, carte et horaires — à connecter à l'API (module 13)."
    />
  );
}
