import { PageProvisoire } from "@/components/PageProvisoire";

export const metadata = { title: "Services" };

export default function PageServices() {
  return (
    <PageProvisoire
      titre="Nos services"
      description="Le catalogue complet (impression grand format, textile, conception graphique) arrive avec le module Vitrine & Catalogue — phase 2 du développement."
    />
  );
}
