import { PageProvisoire } from "@/components/PageProvisoire";

export const metadata = { title: "Ressources graphiques" };

export default function PageRessources() {
  return (
    <PageProvisoire
      titre="Espace ressources graphiques"
      description="Logos vectorisés, fichiers PSD, templates — module Ressources, phase 5 du développement."
    />
  );
}
